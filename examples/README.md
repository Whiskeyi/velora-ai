# Velora integration examples

- `vite-sse-chat`: a minimal Vite client with a local POST SSE endpoint.
- `next-tool-approval`: a Next.js agent flow that pauses for an explicit tool decision.

Build `@velora-ai/react` from the repository root first, then run `npm install`
and `npm run dev` inside either example.
