---
"@velora-ai/react": minor
---

Add scalable message windowing, reconnectable SSE transport primitives, typed
tool-call events, safe rich-content defaults, complete locale coverage, semantic
style slots, and server-safe package subpath exports.
