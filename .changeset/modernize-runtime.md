---
"@velora-ai/react": minor
---

Add a headless agent runtime with request-safe message projection, idempotent
reconnectable SSE transport, tool approval, versioned persistence, adaptive
message windowing, split rich-content styles, complete locale coverage, semantic
style slots, and server-safe package subpath exports.
