name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: voidzero-dev/setup-vp@v1
        with:
          node-version: "22"
          cache: true
          run-install: false
      - run: vp install --frozen-lockfile
      - run: vp check
      - run: vp test run
      - run: vp pack
      - run: npm run verify:package
      - run: npm run verify:showcase
      - run: npm pack --dry-run --workspace @velora-ai/react
      - run: vp build
      - run: npm run verify:site-css
